# django-user-auth

The files here are a complete project setup of a Django web framework example that employs the use of an Account app that authenticates and creates a Django User as well as a custom User Model that extends from the Django User Model.
